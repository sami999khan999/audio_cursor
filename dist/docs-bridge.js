// Auto-generated by build.js — do not edit directly
// Built at: 2026-09-01T11:42:20.179Z

// ─── content/docs-bridge.js ─── 
// Audio Cursor — Google Docs Main-World Bridge
// Runs in the main page world (window context) to interact directly with Google Docs' internal event pipeline.
(() => {
    'use strict';

    let lastExtractedDocsText = '';

    // Intercept copy events in the main world where Google Docs populates clipboardData
    document.addEventListener('copy', (e) => {
        if (!e.clipboardData) return;

        const originalSetData = e.clipboardData.setData.bind(e.clipboardData);
        e.clipboardData.setData = function(format, data) {
            if (format === 'text/plain' || format === 'text') {
                lastExtractedDocsText = data || '';
                window.dispatchEvent(new CustomEvent('__AUDIO_CURSOR_DOCS_SELECTION__', {
                    detail: { text: lastExtractedDocsText }
                }));
            }
            return originalSetData(format, data);
        };
    }, true);

    // Listen for extraction requests from the isolated content script
    window.addEventListener('__AUDIO_CURSOR_REQUEST_DOCS_SELECTION__', () => {
        lastExtractedDocsText = '';
        try {
            document.execCommand('copy');
        } catch (_) {}

        window.dispatchEvent(new CustomEvent('__AUDIO_CURSOR_DOCS_SELECTION__', {
            detail: { text: lastExtractedDocsText }
        }));
    });
})();


